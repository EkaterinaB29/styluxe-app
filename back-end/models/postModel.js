const db = require('../config/db');

const Post = {
    create: async (postData) => {
     
    },
    update: async (postId, postData) => {
    
    },
    delete: async (postId) => {
     
    },
    findById: async (postId) => {
        
    },
    findAll: async () => {
    
    }
};

module.exports = Post;
